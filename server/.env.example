# Production only: provide these through your service manager, never commit real values.
DATABASE_URL=postgres://pond:CHANGE_ME@127.0.0.1:5432/pond
PHX_HOST=pond.example.org
PORT=4000
# Generate using: mix phx.gen.secret
SECRET_KEY_BASE=CHANGE_ME
